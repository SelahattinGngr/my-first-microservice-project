server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://service-loadbalancer-postgersql:5432/AuthDB
  jpa:
    hibernate:
      ddl-auto: update
